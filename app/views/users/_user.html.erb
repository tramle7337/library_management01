<div class="row">
  <div class="col-md-8">
    <table class="table">
      <thead>
        <tr>
          <th class="active width30"><%= t ".avatar" %></th>
          <th class="text-center active width35"><%= sort_link(@q, :name, t(".name")) %></th>
        </tr>
      </thead>
    </table>
  </div>
</div>

<% @users.each do |user| %>
  <div class="row">
    <div class="col-md-8">
      <table class="table table-hover">
        <tr>
          <td class="gravatar_size">
            <%= gravatar_for user, size: Settings.gravatar.small_size %>
          </td>
          <td class="text-center name_width width50">
            <%= link_to user.name, user %>
          </td>
        </tr>
      </table>
    </div>
  </div>
<% end %>

